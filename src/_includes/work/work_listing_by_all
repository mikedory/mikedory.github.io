<div id="all" class="all post-group post-group-all">
{% for category in page.categories %}
    <h1 class="{{ category.tag }} page-header">
        <a href="#{{ category.tag }}" class="clickable">{{ category.title }} &raquo;</a>
    </h1>

    <!-- get three portfolio posts -->
    {% assign posts = site.tags.[category.tag] %}
    {% if posts %}
        {% for post in posts limit:3 %}
        {% capture modulus %}{{ forloop.index0 | mod:3 }}{% endcapture %}
        {% comment %} <!-- shell out the row structure --> {% endcomment %}
        {% if modulus == "0" %}
        <div class="row">
        {% endif %}
            <div class="span4">
                <article class='{{ post.class }}' >
                {% comment %} <!-- the post's title --> {% endcomment %}
                <h2><a href="{{ post.url }}">{{ post.title }}</a></h2>
                {% comment %} <!-- if there's a thumb, load both color and b/w versions --> {% endcomment %}
                {% if post.thumb %}
                <a href="{{ post.url }}" class="bwrollover">
                    <img src="http://dory-magickly.herokuapp.com?src={{ post.thumb }}&grayscale=true&thumb=300x200%23" class="gray"/>
                    <img src="http://dory-magickly.herokuapp.com?src={{ post.thumb }}&thumb=300x200%23" class="color" style="display:none"/>
                </a>
                {% endif %}
                {% comment %} <!-- blurb! --> {% endcomment %}
                {{ post.excerpt }}
                {% comment %} <!-- read more? --> {% endcomment %}
                <!-- <a href="{{ content_url(res.url) }}">Read more &raquo;</a> -->
                </article>
            </div>
        {% if modulus == "2" or forloop.last %}
        </div>
        {% endif %}

        {% endfor %}
    {% endif %}

{% endfor %}
</div>